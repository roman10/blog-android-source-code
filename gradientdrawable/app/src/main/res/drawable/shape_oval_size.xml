<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
  android:shape="oval">

  <stroke android:width="2dp"
          android:color="@color/colorAccent"
          android:dashWidth="4dp"
          android:dashGap="10dp"/>

  <!--The shape scales to the size of the container View proportionate to the dimensions defined here,
  by default. When you use the shape in an ImageView, you can restrict scaling by setting the
  android:scaleType to "center".
  -->
  <size android:height="30dp"
        android:width="50dp"/>

  <!--Padding to apply to the containing View element (this pads the position of the View content,
  not the shape).
  For example, if this oval is set to the background of an ImageView, and the paddings will be applied
  to position the image resource (view content) of the image view. -->
  <padding android:left="20dp"
           android:right="20dp"
           android:top="2dp"
           android:bottom="2dp"/>

</shape>